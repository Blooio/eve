---
"eve": patch
---

Multi-line paste in the dev TUI prompt: pasting multi-line text now inserts it intact via bracketed paste, instead of truncating at the first line. Embedded newlines show inline as `⏎` and are preserved on submit.
